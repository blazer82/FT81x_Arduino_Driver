name: build

on:
  pull_request:
    paths-ignore:
      - "hardware/**"
  push:
    paths-ignore:
      - "hardware/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build example sketches
        run: bash ci/build.sh
